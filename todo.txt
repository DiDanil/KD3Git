В собраных правилах записывать инфу о дате сборки, ветке, хешу коммита? +
Обновление KD3Git перед сборкой/разборкой (опциональное через настройки)
Настройки
Настройки на пробелы и переносы строк, игнор пустого файла получающегося в результате разбиения на несколько коммитов
	git config --global core.quotepath false
	git config --global core.autocrlf false
	git config --global core.safecrlf false
	
	git config --global core.autocrlf true
	git config --global core.whitespace trailing-space,space-before-tab


	git config --get core.whitespace
	git config --global protocol.file.allow always // Или вынести кд3гит на локальный сетевой GitLab, Bitbucket, для добавления сабмодуля?й
	
	format.pretty=%h %ad | %s%d [%an]
	log.date=short


Автоматическая начальная сборка репозитория (гитигнор, настройки путей, настройки репозитория, скрипты)
Много текстового мусора в консоли

Глюки параметризации (при перезапуске скрипта например) +
При сборке правил по ветке может быть ситуация с расхождением собираемой ветки от состояния ветки в общем репозитории
Удаленного репозитория может и не быть

Так себе обрабатываются изменения в пробелах - +-
ЗаполнитьПравилаКонвертацииПредопределенныхДанных.txt - при индексации изменений подтягивает вставку конвертации значений со строчки выше начала изменений

Многострочный коммит

Хук с переименованием веток на мердж

Обработка выгрузки правил из рабочей базы !!!!
	Подставить путь выгрузки в обработку выгрузки (генерить новую обработку подставляя пути?)
	Подставить в скрипт запуска пути - пароли - путь к обработке выгрузки
	
	С обновлением main
	В пакетном режиме
	


Отправка правил
	При отправке предложить запустить скрипт создания ветки
	Шаблон коммента/Подтягивать последний сделанный коммент?
	
	Пулл/Пуш
	А может я не хочу делать пулл перед коммитом в локал? Как то опционально перед разбором делать? Принудиловка по различию авторов текущего и последнего коммитов?
	
Как то подтягивать Gi с шаблона